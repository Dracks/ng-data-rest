import { NgModule } from '@angular/core';
import { RestManagerService } from "./rest-manager.service";

@NgModule({
    providers: [
        RestManagerService
    ]
})
export class RestManagerModule{}
